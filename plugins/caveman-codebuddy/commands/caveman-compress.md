---
description: Compress a natural language memory file into caveman format
argument-hint: "<filepath>"
---

Compress the file at $1 into caveman format. Preserve all technical substance, code, URLs, and structure. Backup original as FILE.original.md. The compression scripts live in the `caveman-compress` skill's `scripts/` directory. Run: `python3 -m scripts <absolute_filepath>` from the `skills/caveman-compress/` directory.
