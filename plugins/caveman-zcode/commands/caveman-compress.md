---
description: Compress a natural language memory file (CLAUDE.md, todos, preferences) into caveman format
argument-hint: "<filepath>"
---

Compress the file at $1 into caveman format. Preserve all technical substance, code, URLs, and structure. Backup original as FILE.original.md. Use the compression scripts in scripts/ directory. Run: python3 -m scripts $1